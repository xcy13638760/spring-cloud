server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          searchPaths: repository
          uri: https://github.com/xcy13638760/spring-cloud-config-file
          username: ''
          password: ''
